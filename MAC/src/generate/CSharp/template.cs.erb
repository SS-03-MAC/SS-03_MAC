using System;
using Mac.Types;


[TableName("<%= table_name %>")]
public class <%= name %> : BaseModel {
  <% fields.each do |field| %>
    [DatabaseField("<%= field["type"] %>")]
    public <%= field["type"] %> <%= field["name"] %> { get; set; }
  <% end %>
}
